<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hambola">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>


    <application
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.NoActionBar">
        <activity android:name=".prePathFinder"/>
        <activity android:name=".pathfinder" />
        <activity android:name=".tictac" />
        <activity
            android:name=".drawer"
            android:label="Agile Dev"
            android:theme="@style/NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".winners" />
        <activity android:name=".settings" />
        <activity android:name=".splashScreen" />
        <activity android:name=".tambola" />
        <activity android:name=".members"/>
        <activity android:name=".join" >

        </activity>
        <activity android:name=".createGame" />
        <activity android:name=".MainActivity" />

    </application>

</manifest>